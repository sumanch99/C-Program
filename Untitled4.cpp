#include<iostream>
#include<string.h>
using namespace std;
int main()
{
	char v[5],x[6],y[12];
	gets(v);
	cout<<"hh";
	gets(x);
	cout<<"------------";
	strcat(v,x);
	puts(v);
	return 0;
}
